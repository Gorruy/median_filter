#ifndef MDN_FLTR_HDR
#define MDN_FLTR_HDR

int median_filter(int* numbers, long window_size);

int partition(int* number, int left, int right);

int print_bin(int number, long window_size);

int k_order(int number[], int k, long size);

#endif
